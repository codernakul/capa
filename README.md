**Comprehensive Academic Performance Analysis**

My task was to create a SQL database by extracting information from the consolidated PDF of grade cards of all departments.

I found patterns within the data extracted from the PDF and according to those wrote two scipts which can consolidate all the required info into different text files.

Then a simple script pushed it into a SQLite database.
